{% extends 'base.html.twig' %}

{% block title %}Vendeur{% endblock %}

{% block body %}
    <h1>Vendeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendeur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vendeur.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ vendeur.surname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ vendeur.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ vendeur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vendeur_index') }}">back to list</a>

    <a href="{{ path('app_vendeur_edit', {'id': vendeur.id}) }}">edit</a>

    {{ include('vendeur/_delete_form.html.twig') }}
{% endblock %}
